// Static NPC data has been moved to src/seed-data/npcs.ts for seed routes.
// At runtime, NPCs are loaded from DB via loader.ts → /api/game-data.
// This file is intentionally left empty — no static fallbacks.
export {};
