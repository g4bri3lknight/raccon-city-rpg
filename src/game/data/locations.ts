// Static location data has been moved to src/seed-data/locations.ts for seed routes.
// At runtime, locations are loaded from DB via loader.ts → /api/game-data.
// This file is intentionally left empty — no static fallbacks.
export {};
