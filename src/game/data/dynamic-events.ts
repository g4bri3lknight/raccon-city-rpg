// Static event data has been moved to src/seed-data/events.ts for seed routes.
// At runtime, events are loaded from DB via loader.ts → /api/game-data.
// This file is intentionally left empty — no static fallbacks.
export {};
