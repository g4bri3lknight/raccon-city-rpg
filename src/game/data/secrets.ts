// Static secret room data has been moved to src/seed-data/ (not yet created, data in seed-all).
// At runtime, secret rooms are loaded from DB via loader.ts → /api/game-data.
// This file is intentionally left empty — no static fallbacks.
export {};
